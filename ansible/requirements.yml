---
# authorized_key lives in ansible.posix; git_config in community.general; the security-group
# play needs amazon.aws. Install with:
#   ansible-galaxy collection install -r requirements.yml
collections:
  - name: ansible.posix
  - name: community.general
  - name: amazon.aws
