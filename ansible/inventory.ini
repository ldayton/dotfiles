# This box, provisioned in place. Point [dev] at a remote host instead and the same role
# applies there -- nothing in it assumes localhost.
[dev]
localhost ansible_connection=local

[dev:vars]
ansible_python_interpreter=/usr/bin/python3
